# Prompt

Review the synthetic `docs-helper-mcp` source fixture using the Parallax
observation model.

Questions:

- What judgment-free atoms appear in the code and tool schema?
- Does the MCD lens have enough proof to call the artifact malicious?
- What does the agent-risk lens say when an agent can read the tool descriptions
  and call the tools?
