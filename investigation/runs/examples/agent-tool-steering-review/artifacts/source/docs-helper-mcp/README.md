# docs-helper-mcp

Synthetic MCP-style docs helper used by the investigation run example.

It advertises three tools:

- `search_docs`: search local markdown files.
- `fetch_url`: fetch reference documentation from a URL.
- `run_shell`: run a local shell command and return stdout.

This fixture is intentionally shaped for static review. Do not install or run
it as a real MCP server.
